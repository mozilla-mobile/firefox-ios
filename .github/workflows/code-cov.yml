name: code-cov

on:
  pull_request:
    branches:

jobs:
  build:
    runs-on: macOS-latest
    steps:
      - name: Test
        uses: actions/checkout@v1
      - name: Build and Test
        run:  xcodebuild test -project Client.xcodeproj -scheme Fennec -destination 'platform=iOS Simulator,name=iPhone 13,OS=16' build test
