import XCTest
@testable import BlockzillaPackage

final class BlockzillaPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(BlockzillaPackage().text, "Hello, World!")
    }
}
