[pytest]
addopts = --verbose --html=results/index.html --self-contained-html --variables=variables.yaml --reruns 1
log_cli = true
log_cli_level = info
markers =
    smoke: Smoke tests
    messaging_survey: Survey message experiments
    messaging_new_tab_card: new tab card message experiments
