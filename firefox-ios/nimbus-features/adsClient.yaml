# The configuration for the adsClient feature
features:
  ads-client:
    description: This property defines if the rust ads client is enabled
    variables:
      status:
        description: If true, we will enable the rust ads client
        type: Boolean
        default: true
    defaults:
      - channel: beta
        value:
          status: true
      - channel: developer
        value:
          status: true