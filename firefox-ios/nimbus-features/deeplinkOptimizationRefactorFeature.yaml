# The configuration for the deeplinkOptimizationRefactorFeature feature
features:
  deeplink-optimization-refactor-feature:
    description: >
      The feature flag to manage the rollout of deeplink optimization feature.
    variables:
      enabled:
        description: >
          Variable description
        type: Boolean
        default: false
    defaults:
      - channel: beta
        value:
          enabled: true
      - channel: developer
        value:
          enabled: true
