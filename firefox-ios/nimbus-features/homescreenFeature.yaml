features:
  homescreenFeature:
    description: The homescreen that the user goes to when they press home or new tab.
    variables:
      sections-enabled:
        description: >
          This property provides a lookup table of whether or not the given section should be enabled.
          If the section is enabled, it should be toggleable in the settings screen, and on by default.
        type: Map<HomeScreenSection, Boolean>
        default:
          {
            "jump-back-in": true,
            "recent-explorations": false,
          }
      pocket-sponsored-stories:
        description: >
          This property defines whether pocket sponsored
          stories appear on the homepage.
        type: Boolean
        default: false
    defaults:
      - channel: developer
        value: {
          "sections-enabled": {
            "jump-back-in": true,
            "recent-explorations": false,
          },
          "pocket-sponsored-stories": true
        }
      - channel: beta
        value: {
          "sections-enabled": {
            "jump-back-in": true,
            "recent-explorations": false,
          },
          "pocket-sponsored-stories": false
        }
  prefer-switch-to-open-tab:
    description: >
      Feature for managing the roll out of functionality that prefers switching to an already open tab instead of opening a duplicate tab.
    variables:
      enabled:
        description: >
          Enables the feature to automatically switch to an existing tab with the same content instead of opening a new one.
        type: Boolean
        default: false
    defaults:
      - channel: beta
        value:
          enabled: false
      - channel: developer
        value:
          enabled: true

enums:
  HomeScreenSection:
    description: The identifiers for the sections of the homescreen.
    variants:
      jump-back-in:
        description: The tabs the user was looking immediately before being interrupted.
      recent-explorations:
        description: The tab groups
